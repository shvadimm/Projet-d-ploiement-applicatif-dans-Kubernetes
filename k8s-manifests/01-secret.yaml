apiVersion: v1
kind: Secret
metadata:
  name: todos-secret
  namespace: todos
  labels:
    app: todos-app
type: Opaque
stringData:
  # Database credentials
  DB_PASSWORD: todopass
  DB_ROOT_PASSWORD: rootpass
  GALERA_BACKUP_PASSWORD: change-me-galera-backup-password
  GALERA_REPLICATION_PASSWORD: change-me-galera-replication-password
  # Flask secret key for session management
  SECRET_KEY: changez-moi-en-production-absolument-necessaire
